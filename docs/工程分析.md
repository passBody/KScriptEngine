# KScript 工程分析文档

> 分析日期：2026-08-29
> 分析方式：四个并行代码审查代理分别深读 model 层、widgets 层、actions/tools/入口层、设计文档体系（12 份 spec、7 份 plan、5 个 SDD 任务目录），本文为汇总。
> 代码规模：28 个 Python 文件，共约 12,400 行（含约 4,000 行内嵌冒烟自测）。

---

## 1. 项目概述

### 1.1 定位

**KScript 是一个用 PyQt5 桌面 GUI 编排"自动操作脚本"的步骤化工具**：把一段段可复用的原子操作（"步骤"）组装成可执行的"步骤列表"，整体打包为 `.kscp` 工程文件（ZIP 归档）保存与分发。

- **解决什么问题**：从示例数据（列表名「打开软件」「清理体力」「检查体力」、变量「莫拉数量」「原石数量」）可推断，需求源头是大量重复性按键/鼠标操作（游戏日常、重复性桌面操作等）。KScript 让**不写代码的人**通过 GUI 编辑步骤卡片来编排操作序列，而不是手写 Python 脚本。
- **工程形态**：纯本地单机应用，无网络/服务端概念。入口 `main.py` → 打开 `.kscp` 工程 → 左侧活动栏 4 棵管理树（步骤列表/变量/步骤模板/资源）+ 中央预览区 + 底部日志面板。

### 1.2 当前状态

- **已成型**：完整的数据模型层、四棵管理树 UI、步骤卡片/列表视图、变量编辑、资源管理、日志系统、图片标注工具、应用图标。
- **关键缺口**：**步骤执行器（运行时引擎）尚未实现**——目前只能编辑与持久化，不能真正"运行"一个步骤列表。这是产品从"编辑器"变成"工具"的最后一步。

---

## 2. 技术栈

| 维度 | 选择 |
|---|---|
| 语言 | Python 3.14 |
| GUI | PyQt5（QWidget 体系 + QGraphicsView 卡片视图 + QSS 样式） |
| 持久化 | 自研 `.kscp` 格式：ZIP 归档 + JSON + base64 格式串 |
| 依赖 | 标准库为主；numpy / Pillow 为可选依赖（try-import 降级） |
| 测试 | 无测试框架；每模块 `__main__` 冒烟自测（assert 驱动，`python -m xxx` 运行） |
| 版本管理 | **无 git 仓库**（工程目录非 git，SDD 流程已做非 git 适配） |
| 开发流程 | Superpowers SDD：spec → plan → 子代理任务 → 评审 → 终审 ledger |

---

## 3. 架构设计

### 3.1 分层

```
main.py（参数解析）
   └── widgets/main_widget.py  MainWindow（landing / 工程双态窗口）
          ├── widgets/   UI 层（9,000+ 行）
          │     ├── 4 棵管理树: step_list_tree / variable_tree / step_tree / resource_tree
          │     ├── 步骤视图: step_list_view (QGraphicsView) + step_card (QFrame)
          │     ├── step_io_widget（轻量数据对象，非 QWidget）
          │     ├── log_widget / image_overlay
          ├── model/   数据层（3,000+ 行）
          │     ├── kscp_package / variable_tree / step_list_store（容器三兄弟）
          │     ├── project_variable / step_manager（值对象/工厂）
          │     ├── step_list（有序步骤容器）
          │     ├── log_model（单例日志）
          │     ├── step.py（Step 基类，do() = input→run→output→offset）
          │     ├── step_io.py（StepIOWidget，轻量数据对象）
          │     └── point_timeline（标注计时）
          └── actions/  步骤类（模板，随 .kscp 分发）
                ├── base.py（re-export 垫片 → model.step）
                └── 控制流程/time_delay.py（唯一内置步骤）
```

### 3.2 核心概念模型

```
                        ┌──────────────── .kscp 工程包 (ZIP) ────────────────┐
  StepManager ──动态导入──> actions/**/*.py（步骤模板类，随包分发）            │
        │                        ▲                                          │
        │  create_step / from_format_string                                  │
        ▼                        │                                          ▼
   Step 对象（model/step.py）              step_list.json ──> StepListStore ──> StepList ──> Step
        │  io / status / enabled / do()            (组+列表嵌套字典)   (有序 Step 列表)
        ▼
   StepIOWidget（输入槽存 "{{变量名}}"/常量，输出槽存变量名）
        │  resolve_inputs / write_outputs / picker / gen_widget
        ▼
   variables.json ──> VariableTree ──> ProjectVariable (string / number / image)
        │                                     │
        └── image 类型引用 ───────────────────> assets/ 资源文件 ── ResourceTreeWidget

   LogModel（单例）<── 所有模型错误/操作 ── 贯穿全部层，LogWidget 展示于底部
```

要点：

- **`.kscp` 是唯一工程载体**：含 ① `actions/**/*.py`（步骤模板源码）② `step_list.json`（列表名 → 步骤 base64 串数组）③ `variables.json`（变量树）④ `assets/`（资源）。`KscpPackage` 只做字节存取，**刻意不解析包内 JSON**。
- **Step 是执行单元**：`do()` 编排 input→run→output，返回**偏移量**（1=下一个、2=跳一个、0=重调用自身），由未来执行器消费；`enabled` 参与过滤。
- **格式串是步骤的序列化协议**：UTF-8 JSON → urlsafe base64 去填充。步骤跨列表复制粘贴、跨工程打包全靠它。
- **名字存在字典键上**：StepList 自身无名字，名字在 StepListStore 的键上；StepIOWidget 不继承 QWidget，是纯数据对象。

### 3.3 数据流

- **打开**：`KscpPackage.from_kscp` → `VariableTree.from_json(variables.json)` + `StepManager.load()`（动态导入包内 actions/*.py）+ `StepListStore.from_json(step_list.json)` → UI 编辑。
- **保存**：各模型 `to_json_bytes()` → `pkg.write_file` → `pkg.save()`（mkstemp + os.replace **原子写盘**）。
- **执行（预留）**：`StepListStore.all_do_methods()` 按插入序 DFS 整合全部 enabled 步骤的 do → 逐条调用，偏移量决定跳转。

### 3.4 序列化协议栈

| 层 | 格式 | 细节 |
|---|---|---|
| 工程包 | ZIP（ZIP_DEFLATED） | 条目按名排序保证确定性；目录以 `/` 结尾（空目录靠它存活）；原子写入 |
| 包内 JSON | UTF-8（ensure_ascii=False） | `variables.json` 嵌套字典；`step_list.json` 组+列表字典 |
| 对象格式串 | urlsafe base64（去 `=` 填充） | ProjectVariable：`{"t","d"}`；Step：`{"name","in","out","io","run","tag"}`（缺 run/tag 向后兼容默认）；io：`{"it","iv","ot","ov"}` |

---

## 4. 模块详解

### 4.1 model/（11 个文件，约 3,700 行）

| 模块 | 行数 | 职责 |
|---|---|---|
| kscp_package.py | 460 | `.kscp` 包内存模型：扁平路径→字节 + 显式空目录集；路径归一化拒 `..`；原子保存 |
| project_variable.py | 424 | 单个变量：类型注册表 + 策略处理器（string/number/image）；base64 格式串；`valid` 标记 |
| variable_tree.py | 480 | 全部变量容器：嵌套字典 ↔ 对象；与 KscpPackage 同构的路径不变量 |
| log_model.py | 185 | 全局日志单例：5 级别（对齐 Python logging 数值）、监听器回调、text/json 导出 |
| step_io.py | ~740 | 步骤输入/输出设置（轻量数据对象，非 QWidget）；模块顶层不导入 PyQt5，Qt 延迟到 gen_widget 等方法内（2026-08-29 自 widgets/ 迁入） |
| step.py | ~476 | Step 基类 + StepStatus：do() 编排、槽类型推导、格式串往返；模块顶层不导入 PyQt5（2026-08-29 自 actions/base.py 迁入） |
| step_manager.py | 584 | 步骤模板工厂：动态导入包内 actions/*.py；唯一模块名前缀防跨工程串包；模板增删/剪贴板 |
| step_list.py | 279 | 有序 Step 列表：增删插移、do 快照导出、格式串往返（坏条目报「第 N 条」） |
| step_list_store.py | 507 | 总步骤列表容器：嵌套组+列表、walk（先序含空组）、`all_do_methods()` |
| point_timeline.py | 92 | 坐标点 + 点击间隔计时（供 image_marker 用，纯运行时） |
| `__init__.py` | ~30 | 门面（导出 9 类，含 Step/StepStatus/StepIOWidget；StepManager/StepList/StepListStore 走子模块路径） |

依赖方向严格单向：`kscp_package ← project_variable ← variable_tree ← step_io`；`step → step_io + log_model + project_variable`；`step_manager → step + kscp_package + log_model + variable_tree`；`step_list → step`；`step_list_store → step_list`。模型内部无环，**导入任意 model 模块均不拉入 PyQt5/widgets**（2026-08-29 修复验证）。

### 4.2 actions/（步骤模板层）

- **base.py（约 20 行）**：re-export 垫片 → `model.step`。保留旧 .kscp 包内模板（`from actions.base import Step`）的兼容加载路径。
- **控制流程/time_delay.py（114 行）**：唯一内置步骤。`seconds <= 0` 置 ERROR + 日志（不抛异常）；`seconds > 0` 则 `time.sleep`。已改为 `from model.step import ...`。
- `__init__.py` 为静态 import 登记——新增步骤需手动改三处 `__init__`。

### 4.3 widgets/（11 个文件，约 9,000 行）

| 模块 | 行数 | 职责 |
|---|---|---|
| main_widget.py | 1145 | `MainWindow`：landing/工程双态、工具栏、活动栏（VSCode 风格）、4 树 + 预览栈 + 日志的组装、共享变量树/StepManager 接线、`main()` 入口 |
| step_list_tree_widget.py | 1608 | 步骤列表管理树：组/列表增删改查、拖拽排序（=剪切粘贴同构+事务性预检）、勾选激活、错误红标；**walk 序=显示序=执行序** |
| step_list_view.py | 1234 | 步骤卡片横向滚动视图（QGraphicsView）：滚轮横滚、悬停 1.06 中心缩放动画、多选、右键菜单、模板选择对话框 |
| step_card.py | 389 | 步骤卡片：激活按钮、签名框、自定义视图、输入/输出 GUI；背景色随状态/合法性变化；动态属性 `active` 避开 Q_PROPERTY `enabled` |
| step_io_widget.py | ~20 | re-export 垫片 → `model.step_io`（2026-08-29 迁移后保留兼容路径） |
| step_tree_widget.py | 668 | 步骤模板树（只展示不创建实例）+ StepInfoPanel 信息面板 |
| variable_tree_widget.py | 1174 | 变量树 + 类型编辑器注册表 + 创建对话框 + 撤销栈（上限 100） |
| resource_tree_widget.py | 658 | 资源树 + 三页预览 + `pick_resource` 选择模式 |
| log_widget.py | 198 | 日志面板：5 级过滤、清空、导出 |
| image_overlay.py | 187 | 全屏图片遮罩：滚轮缩放/拖拽平移（资源与变量预览共用） |

### 4.4 tools/ 与 icon/

- **tools/image_marker.py（563 行）**：图片标注/坐标拾取工具。全屏 1:1 显示图片让用户点选坐标，返回带点击间隔的 `PointTimeline`，供未来"点击自动化"步骤使用。6 种输入类型自动适配（str/bytes/QImage/QPixmap/PIL/numpy），4 种模式（dot/rect/dots/show），本地 QEventLoop 代替 `QDialog.exec()`（后者会复位全屏）。**目前无任何调用者**。
- **icon/make_icon.py（115 行）**：程序化生成应用图标（蓝紫渐变+层叠卡片+播放三角），输出 ico/png/svg 三格式。

---

## 5. 开发流程与实现状态

### 5.1 流程：spec → plan → SDD

1. **spec**（docs/superpowers/specs/，12 份）：日期前缀命名，固定结构（目标/概念/接口表/校验错误表/测试/TDD 冒烟清单/非目标）。
2. **plan**（docs/superpowers/plans/，7 份）：含完整可转录代码块 + Global Constraints（绝对导入、禁 future annotations、禁管道等）。
3. **SDD 执行**（.superpowers/sdd/，5 个任务目录）：task-brief（awk 从计划摘录）→ 子代理实施（RED 真实失败输出 → GREEN 冒烟）→ 评审 → review-package（程序化 diff）→ 终审 Rulings 记入 ledger。非 git 适配：无 commit、无分支，冒烟+自检验证替代。

### 5.2 实现状态：12 份 spec 全部实现

| 状态 | 明细 |
|---|---|
| ✅ 全部落地 | kscp-package、project-variable、resource-tree-widget、step-io-widget、variable-tree、log-model、step-base-class、step-manager、step-tree、variable-tree-widget、step-list、step-widgets |
| ⚠️ 流程记账缺口 | log-system 与 variable-tree 两个 plan **无对应 sdd 目录**（早期未走 SDD ledger）；step-tree 的 plan 检查框未勾（sdd 已完成） |
| ❌ 唯一"规格已写、实现未落地" | **执行器**——甚至没有 spec，只有 `all_do_methods()` 导出与 `do()` 偏移量语义的预留 |

SDD 终审记录良好：5 个目录全部通过终审（含 2 个 "With fixes"），最长的是 step-widgets（7+2 任务、19/19 冒烟全绿、多轮 bug 修复）。

---

## 6. 代码质量评估

### 6.1 优点（值得保留的设计）

- **异常即契约**：ValueError=非法输入、FileExistsError=冲突、FileNotFoundError=缺失，语义全项目一致。
- **事务性操作**：`insert_format_strings` 先全部解码成功再统一插入；拖拽移动先验证格式串可还原再删源（修过"拖拽哪个丢哪个"的数据丢失 bug）。
- **原子落盘**：mkstemp + os.replace，失败清理临时文件。
- **向后兼容**：格式串缺 run/tag 键默认补值；`ensure_ascii=False` + 无填充 urlsafe base64 保证输出稳定。
- **错误可定位**：格式串坏条目报「第 N 条」；未注册注解报步骤名/字段名/支持列表。
- **防坑积累**：动态属性避开 Q_PROPERTY、blockSignals+try/finally 重建防 use-after-free、动画 finished 即清理防 RuntimeError、弱引用防自引用环、幽灵 proxy 回归防线——每条坑都有注释 + 冒烟断言记录。
- **Windows 平台差异显式注释**（_read_local 预检、模块名前缀防串包）。
- **冒烟测试真实**：RED 阶段要求真实失败输出，GREEN 要求 EXIT=0 且绝不经过管道（防吞退出码）。

### 6.2 问题清单（按严重度分级）

#### 🔴 高

| # | 位置 | 问题 |
|---|---|---|
| 1 | ~~model/step_manager.py → actions/base.py → widgets/step_io_widget.py~~ | ✅ **已修复（2026-08-29）**：~~分层倒置~~。`Step`/`StepStatus` 已迁至 `model/step.py`、`StepIOWidget` 已迁至 `model/step_io.py`（模块顶层不导入 PyQt5，Qt 延迟到 gen_widget/info_widget 等方法内导入）；`actions/base.py` 与 `widgets/step_io_widget.py` 保留为 re-export 垫片（旧 .kscp 包内模板 `from actions.base import Step` 仍可加载）。验证：`import model.step_manager` 后 `sys.modules` 无 PyQt5/widgets，全部冒烟通过 |
| 2 | actions/控制流程/time_delay.py:56 | `time.sleep` 阻塞 GUI 线程：步骤执行若发生在主线程，延时几秒即冻结界面；全项目无线程/异步/QTimer 执行方案（执行器未实现） |
| 3 | ~~model/step_io.py:129-173~~ | ✅ **已修复（2026-08-30）**：~~槽值非 str 即崩溃~~。`_decode` 校验 it/ot/iv/ov 元素全为 str，运行时校验/展示路径防御非 str（见第 8 节第 2 条） |
| 4 | — | **执行器缺失**（见第 7 节），产品无法"运行" |

#### 🟡 中

| # | 位置 | 问题 |
|---|---|---|
| 5 | ~~kscp_package.py / variable_tree.py / step_list_store.py~~ | ✅ **已修复（2026-08-30）**：~~路径归一化四处拷贝~~。新建 `model/path_util.py` 统一收敛（折叠空段与 `.`、拒绝 `..`），四个模块全部委托（见第 8 节第 5 条） |
| 6 | ~~kscp_package.py:133-141~~ | ✅ **已修复（2026-08-30）**：~~from_kscp 不归一化~~。所有 zip 条目经 path_util 归一化，含 `..` 条目加载即拒绝 |
| 7 | ~~kscp_package.py:324-352；variable_tree.py:309-335~~ | ✅ **已修复（2026-08-30）**：~~move 自嵌套~~。两处 move 均拒绝目标在源子树内 |
| 8 | main_widget.py:806-809 | 反复新建/打开工程时旧管理树控件仅 removeWidget 不 deleteLater，widget 堆积风险 |
| 9 | step_card.py:52 | cards.qss 在 import 时模块级读取，文件缺失/损坏 → 整个 widgets 包导入失败 |
| 10 | step_card.py:194-197 | 每次按键触发 io_changed → 全量序列化落盘，无防抖（每键一次写盘） |
| 11 | model/step.py:197-198 | `from_format_string` 跨类访问 `io._input_type`/`io._output_type` 私有属性，破坏封装 |
| 12 | ~~log_widget.py:104-108~~ | ✅ **已修复（2026-08-30）**：~~LogWidget 全量重建~~。改为增量追加，回看时不被强制拉底；清空/筛选变化才全量重建 |
| 13 | ~~variable_tree.py:152-166；step_list_store.py:158-171~~ | ✅ **已修复（2026-08-30）**：~~from_json 递归无上限~~。两处 `_walk` 深度上限 128，json.loads 的 RecursionError 转 ValueError |
| 14 | project_variable.py:281-285 | 定义 `__eq__` 未定义 `__hash__` → 对象不可哈希，不能作 dict key/set 元素 |
| 15 | log_model.py:136-141 | `_notify` 静默吞监听器异常，UI 刷新崩溃无声无息 |
| 16 | step_manager.py:189-191 | 模板写盘回滚 `except Exception: pass`——remove 失败则孤儿文件留在包内，下次 load 又注册，状态不一致 |
| 17 | ~~model/step.py:133-138~~ | ✅ **已修复（2026-08-30）**：~~run() 返回值无校验~~。`do()` 校验偏移量必须为 int（bool 排除），非法 → ERROR + ValueError 重抛。**负值合法**：执行器 = 程序计数器 + 偏移，负值回跳（用户确认的汇编式跳转语义） |
| 18 | demo_resource_tree.py vs sample.kscp | 演示脚本 `--save` 产物（空 store、无 actions/、封面2.png）与实际 sample.kscp（2 列表 9 步骤、完整变量树、封面3.png）不一致——sample 并非由 demo 生成 |
| 19 | model/step.py:108-113 | 基类自身 `input_class=object`，`Step.create_default()` 对 object 抛 TypeError 而非文档承诺的 ValueError |

#### 🟢 低

| # | 位置 | 问题 |
|---|---|---|
| 20 | main_widget.py:444-468 | `PlaceholderManagementTree` 与基类 `_tree/_preview` 缓存为死代码；四子类各自重复实现懒缓存 |
| 21 | 多处 | 重复代码：橙色三横线图标绘制 ×3、`_ClickableLabel` ×2、路径归一化 ×4、_act_paste 等菜单逻辑树间同构 |
| 22 | 多处 | 跨类私有访问常见：`_store._root`（step_list_tree_widget ×4）、`_view._cards`、`_find_item`、`_save()` |
| 23 | step_list_view.py:449-450 | `obj in self._cards` + `index()` 双重 O(n) 线性查找，卡片多时悬停/缩放路径性能风险 |
| 24 | 全部文件 | 约 4,000 行冒烟测试内嵌生产模块 `__main__`（step_manager 冒烟占 46%），无独立测试文件；测试间全局类型注册表污染 |
| 25 | log_model.py:55-67 | 手写单例可直接 `LogModel()` 绕过；测试专用 `_reset_instance` 留在生产代码 |
| 26 | main.py:9-13 | 参数解析过宽：未知 flag 被当路径；`demo_resource_tree.py --save --check` 会把文件存成 `--check`；无 --help |
| 27 | tools/image_marker.py:88-90 | 模块导入副作用：无条件 `sys.path.insert(0, _ROOT)` 污染全局路径 |
| 28 | time_delay.py:33 | 默认值 0.0 本身非法（≤0 报错），新建的延时步骤默认处于错误态，交互反直觉 |
| 29 | actions/__init__.py | 静态 import 登记步骤，无动态发现；新增步骤需改三处 `__init__`，与包内模板双轨易不同步 |
| 30 | sample.kscp / qwer.kscp | 示例工程含疑似调试遗留数据（列表名「新列表555」「1新列表」、tag「测试」），作为示例不够干净 |

---

## 7. 未完成功能与技术债

### 7.1 最大的缺口：执行器

- `StepListStore.all_do_methods()` 导出与 `Step.do()` 偏移量语义（1/2/0）均已就绪，但**没有任何代码真正按序调用 do**。
- 执行器必须解决：GUI 线程阻塞（time.sleep）、状态流转的 UI 反馈（卡片颜色已就绪）、错误中断策略、偏移量跳转语义。
- 建议执行前先补一份执行器 spec（沿用现有 spec 结构）。

### 7.2 未闭环的 UI 流程

1. **`step_selected` 信号无消费者**——「从模板树选模板 → 实例化 → 加入列表」未闭环。
2. 步骤模板的**内容编辑/改名** UI 未做（模型层 `add_template`/`copy_source_templates` 已有）。
3. StepIOWidget 的 `picker` 是临时方案，spec 明示应替换为完整树资源管理器（资源树+变量树整合选择器）。
4. 卡片拖动排序、跨列表移动步骤、步骤搜索/过滤（step-widgets §9 非目标明确列出）。
5. 模板粘贴后的后置处理（粘贴模板到包内 actions 的完整流程）。

### 7.3 流程记账

- log-system、variable-tree 两个 plan 无 sdd 目录；step-tree plan 检查框未勾。
- 16 条 deferred Minor 分诊记录在 ledger 中停车（如冒烟未覆盖 `__iter__`、`add_group("")` 边界）。
- runpy RuntimeWarning（`python -m` 运行冒烟）已知无害但未消除。

---

## 8. 改进建议（按优先级）

> 2026-08-29 修订：以「地基 → 执行器」为序。2026-08-30 完成第 0/1 层与第 2 层
> 除「中断机制」外的全部条目（✅ 标记处）。

### 第 0 层：工程卫生

1. ✅ **git 仓库已初始化**（用户完成）。后续每完成一批改动应提交。

### 第 1 层：数据完整性（全部完成）

2. ✅ **加固格式串解码**（高#3）：`model/step_io.py::_decode` 校验 it/ot/iv/ov 元素全为 str（手工编辑的 .kscp 含数字等 → ValueError 而非渲染时 TypeError）；`_is_input_valid`/`error_reasons`/`_display_name` 运行时防御非 str。
3. ✅ **from_kscp 路径归一化**（中#6）：所有 zip 条目经 `path_util.norm_maybe_root` 归一化，含 `..` 条目加载即拒绝。
4. ✅ **move 自嵌套检查**（中#7）：KscpPackage.move 与 VariableTree.move 均拒绝目标在源子树内（含目标不存在的情况）。
5. ✅ **路径归一化统一**（中#5）：新建 `model/path_util.py`（norm_maybe_root/normalize/validate_name），四处拷贝全部委托；语义收敛为「折叠空段与 `.`、拒绝 `..`」（StepListStore 原拒绝 `.`，已改折叠）。
6. ✅ **from_json 递归深度上限**（中#13）：VariableTree 与 StepListStore 的 `_walk` 深度上限 128 抛 ValueError；json.loads 的 RecursionError 捕获转 ValueError。

### 第 2 层：执行器直接依赖的机制缺口

7. ✅ **状态变更通知机制**：`Step.status` 改为 property，变更即通知 `add_status_listener(cb)` 注册的回调（`cb(step, new_status)`，纯 Python，model 无 Qt）；重复赋值静默；监听者异常不打断状态流转。执行器经此推 UI 刷新（卡片颜色体系已就绪）。
8. ⏳ **中断机制**（源自高#2）：仍待执行器 spec 决策线程模型（QThread worker vs QTimer 分段）；TimeDelay 届时改为可中断的分段 sleep。
9. ✅ **run() 返回值校验**（中#17）：`do()` 校验偏移量必须为 int（bool 显式排除），非法值 → 状态 ERROR + 日志 + ValueError 重抛，output 不执行。偏移量语义（用户确认）：执行器 = 程序计数器 + 偏移，**负值回跳**（如 -1 回到上一步）；偏移量 0/负值导致的**死循环保护**归执行器（spec 决策项）。
10. ✅ **LogWidget 增量刷新**（中#12）：追加新条目不清空重建；用户回看时不被强制拉底（仅原已在底部才跟随滚动）；清空/筛选变化才全量重建。

### 第 3 层：写执行器 spec 并实现

11. **执行器 spec**（沿用 spec→plan→SDD 流程）：需决策——线程模型、中断语义（第 8 条）、偏移量 0 循环保护、错误中断策略（遇错停止/继续）、日志粒度、状态通知→卡片接线（第 7 条已就绪）。

### 第 4 层：后续（不阻塞执行器）

- 防抖落盘（中#10）、清死代码（低#20-21）、冒烟迁 tests/、sample.kscp 清理、参数解析 --help（低#26）、`__eq__` 配套 `__hash__`（中#14）、模板回滚失败告警（中#16）。

---

## 9. 附：文件行数统计

```
 1608  widgets/step_list_tree_widget.py
 1234  widgets/step_list_view.py
 1174  widgets/variable_tree_widget.py
 1145  widgets/main_widget.py
  740  model/step_io.py
  668  widgets/step_tree_widget.py
  658  widgets/resource_tree_widget.py
  584  model/step_manager.py
  563  tools/image_marker.py
  507  model/step_list_store.py
  480  model/variable_tree.py
  476  model/step.py
  460  model/kscp_package.py
  424  model/project_variable.py
  389  widgets/step_card.py
  279  model/step_list.py
  198  widgets/log_widget.py
  187  widgets/image_overlay.py
  185  model/log_model.py
  115  icon/make_icon.py
  114  actions/控制流程/time_delay.py
   95  demo_resource_tree.py
   92  model/point_timeline.py
   14  main.py
    其余 __init__.py 合计约 35
 ─────────────────────────
 合计约 12,400 行（model 约 3,700 / widgets 约 8,500 / actions+其余约 300）
```

---

## 10. 总体评价

这是一个**工程质量异常扎实的早期项目**：分层清晰、序列化协议设计干净、异常契约一致、每处修复都有注释和冒烟断言作回归防线，SDD 流程（spec→plan→任务→评审→终审 ledger）执行得相当完整。

当前最大的风险不是代码质量，而是**方向性缺口**：执行器未实现导致产品闭环未完成。

2026-08-30 地基加固完成：git 已初始化（用户）、路径归一化统一（path_util）、格式串类型校验、from_kscp 归一化、move 自嵌套拒绝、from_json 深度上限、Step 状态通知、do() 偏移量校验、LogWidget 增量刷新——第 8 节第 1/2 层全部落地，仅「中断机制」待执行器 spec 决策线程模型。下一步就是执行器 spec：产品从"编辑器"走向"工具"的最后一块。
